BASE <https://id.example.org/>
PREFIX : <https://id.example.org/schema/>

CONSTRUCT {
    <VolunteeringThing> :concept ?concept .

    ?concept
        :label ?label ;
    .
}
WHERE {
    ?activity :activityCause ?cause .

    ?cause
        :optionDisplayName ?label ;
    .
}
